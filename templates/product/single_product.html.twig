<div class="product-item text-center" >
    <a href="{{path('app_product', {'slug' : product.slug } )}}"><img src="/uploads/{{ product.illustration }}" alt="{{ product.name }}" class="img-fluid"></a>
    <h5>{{ product.name }}</h5><br>
    <span class="product-subtitle">{{ product.subtitle }}</span><br>
    <span class="product-price">{{ (product.price / 100)|number_format(2, ',', '')|default('0.00') }}€</span>


</div>